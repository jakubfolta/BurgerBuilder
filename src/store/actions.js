import * as actionTypes from './reducer.js';

const initialState = {
  ingredients: '',
  totalPrice: null
}

const reducer = (state = initialState, action) => {
  // switch (action.type) {
  //   case actionTypes.:
  //     return {
  //       ...state
  //     }
  // }
  return state;
}

export default reducer;
